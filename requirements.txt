# ============================================================
# KARONEXT — Backend bağımlılıkları (Python 3.13)
# ============================================================

# --- Web / API ---
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.8,<3
pydantic-settings>=2.4,<3
python-multipart>=0.0.9,<1

# --- Veri ---
pandas>=2.2,<4
numpy>=1.26,<3

# --- LLM / EVREN (OpenAI uyumlu) ---
openai>=1.50,<3
httpx>=0.27,<1
python-dotenv>=1.0,<2

# --- RAG / Vektör ---
qdrant-client>=1.12,<2

# --- LangChain (tamamı yerel çalışabilir, dış servis zorunluluğu yok) ---
langchain>=0.3,<0.4
langchain-core>=0.3,<0.4
langchain-openai>=0.2,<0.4
langchain-community>=0.3,<0.4
langchain-qdrant>=0.2,<0.3

# --- Test ---
pytest>=8,<10
pytest-asyncio>=0.23,<1
